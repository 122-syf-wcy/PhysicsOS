# LEGACY INPUT ONLY

本目录只保留旧版 standalone UI 所需的初始 Scene / Renderer 输入。

- 不是 Physics Engine 结果
- 不是真实 Agent / Question Parser 输出
- 禁止保存 R / T / ω / F / trajectory 等派生 Physics Fact
- 正式 Runtime Host 是 Harness Web Client + `ui-physicsos`
- 旧页面仅作迁移参考，不再发展为第二套 Runtime UI
